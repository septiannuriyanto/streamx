import { View, Text } from 'react-native'
import React from 'react'

const Saved = () => {
  return (
   <View className='bg-primary-500 flex-1 px-10'>
      <Text>Saved</Text>
    </View>
  )
}

export default Saved